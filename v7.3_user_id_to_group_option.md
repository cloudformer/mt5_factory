# v7.3 owner_id 从"一个人"升级为"一个组"(option · 未开工, 2026-07-26 记)

> 定位: 前瞻备忘, 不排期。有团队客户(多人共管一份资产)才开工; 在那之前一行代码不写。

## 为什么现在就能记这个文档: 扩展点已经买好保险

v5.6 通电时定死的两条, 正好是本扩展的全部前提:

1. **资产表存的 `owner_id` 本质是"主体编号"**: 资产(strategies / mt5_hosts / trades /
   reconciliations / usage_counters)只回答"我属于几号主体", 不关心主体是一个人还是一群人;
2. **身份→过滤收敛在一处**: `api/src/services/identity.py` 的 `scope_uid()` 是全系统唯一
   把"请求是谁"翻译成"按哪个 owner_id 过滤"的地方; 各端点只拿翻译结果, 不自己判断。

所以升级 = 只改翻译层, 资产表 / 各端点 SQL / 写路径校验 **零改动**:

```
现在:  登录的人 → scope_uid = 本人 id          → WHERE owner_id = uid
将来:  登录的人 → 查成员映射 → 所属主体 id     → WHERE owner_id = 主体 id   (SQL 一字不改)
```

## 演进三级(按需要走到哪级停在哪级)

| 级 | 场景 | 实现 | 新表 |
|---|------|------|------|
| 0(现在) | 一人一主体 | scope_uid 返本人 id | 无 |
| 1(挂靠) | 租户里加只读/操作员 | users 加 `parent_user_id` + `role`(admin/viewer): viewer 用 parent 的 id 查, 写操作 403 — 即 v5.6 文档"前瞻·租户内分权" | 无(users 加两列) |
| 2(真组) | 多人共管、人可属多组 | `groups` + `group_members(user_id, group_id, role)`; scope_uid 返所属组 id(多组则 `owner_id = ANY(...)`) | 2 张 |

- 1 级是 2 级的退化形态(组=parent 本人, 成员=挂靠者), 路径连续, 不推倒重来;
- admin(id 1)逻辑不变: owner 特例仍是"不过滤", 与主体形态无关;
- 资产内写操作规则不变: "资产的 owner(组) == 当前主体"才能写 — 只维护 admin 一个特例。

## 铁律对照(开工时自查)

- 命名唯一: 届时"主体"只能有一个叫法, owner_id 语义文档要同步改口;
- 数据库最小化: 1 级不建表; 2 级两张表都有主键/外键约束执法, 不做权限矩阵;
- 一切可关联: group_members 挂进 users 主键体系, 不造孤岛;
- 服务无状态: 成员关系只在库, 中间件每请求现查(或短缓存), 不留进程态。

## 不做什么

- 不做嵌套组/组织树、不做 RBAC 权限矩阵、不做跨组共享(跨主体可见走市场 visibility, 已有);
- 登录/token(v5.6 后半)先行, 本扩展永远排在它之后。
