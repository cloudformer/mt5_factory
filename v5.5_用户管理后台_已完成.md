# v5.5 用户管理后台(第五步 · 只加不变)【已完成并线上验证 2026-07-25】

> 已落:配置菜单「用户管理」页 — 用户(建/启停/资产计数)· API keys(签发=服务端生成
> sha256 只存哈希明文一次/吊销/恢复)· worker 划拨(选完即提交)· 配置覆盖(写/删=回落默认)。
> 选型定案(2026-07-25 与 Frank):**不上 Flask-Login 全家桶** — v5.6 用 Flask 内建
> session(签名cookie)手写 ~30 行 before_request + bcrypt;
> **角色只有两个(2026-07-25 定)**: owner(=user 1, 全量视角)/ user(管自己的
> 策略/worker/报表/配置的一切) — 不设第三种角色, "admin"只是普通用户名可预留;
> 登录态在 web, **执法必须在 api**(浏览器session/api key/worker key 三种钥匙一个闸)。

> 序列见 v5.1。把用户运营动作页面化——机制是 5.2/5.3 的表,这里只是操作台。

## 功能(一个管理页)

- 用户列表:名字/启停状态/策略数/worker 数;
- 建用户 / 停用启用(users.enabled);
- 签发 api key(明文只显示一次)/ 吊销单把;
- ~~配置管理(user_config 覆盖行编辑)~~ **已撤 2026-07-25**(Frank: 原始 JSON 键值编辑
  不好用) — 表与"空覆盖层+回落全局"机制保留(schema/034),将来做成**配置页式的
  按用户编辑**(选用户 → 表单式改他的生效值),随 v5.6 一起;
- **用量观察**(2026-07-25 加, 只记录不拦截):每用户 今日/累计 回测·AI·建策略(usage_counters)
  + 存量现查(策略数/worker数);
- worker 归属**只读展示**(2026-07-25 与 Frank 定: 归属唯一真相=绑定钥匙的主人,
  announce 每分钟自动对齐, 手动划拨会被钥匙改回且造成两套事实 — 划拨端点已删;
  换归属 = 吊销旧钥匙 + 给新用户发钥匙 + 机器换钥)。

## 数据

无新表——全部复用 5.1~5.3。可选:操作留痕 `user_events`
(仿 mt5_host_events:user_id/event/detail/created_at),记"谁何时被停用/换key/改额度"。

## 权限口径(本步的临时态)

此时认证还没接(5.6 才接),管理页和全站一样"谁能访问网络就能用"——
**与现状的安全水位一致**(内网单人),不新增风险,但也别在 5.6 之前暴露到公网。
5.6 落地后本页只认 owner(user 1);角色仅 owner/user 两种,不做角色体系。

## 风险 / 难度 / 验收

- 风险:**无新增**(见上,与现状同水位);难度:低-中;
- 验收:页面完成 建用户→发key→写/删配置覆盖→划worker 全流程,不再需要手敲 SQL。
