# v1.2 关2 对账 · 未完成部分(活跃)

> 由原 `v1.6_关2对账.md` 拆出(已实现记录见 `v0.6`, as-built);并接收 v0.2 §8 残留。

## 1. 实盘 SL/TP 回写(成绩单已留列, 当前 null)

MT5 成交回写(deals)不带持仓 SL/TP → sync 侧从 positions/历史订单补抓落 trades 表。
补上后 AI 可对照"回测 MAE 分布 vs 实盘实际挂的 SL 距离"直接给调法。

## 2. 成本模型实测校准(≥50 笔配对后)

- 用对账精度偏差(entry_diff_avg 等)取带符号偏差**中位数**+2~3点缓冲 → 复核滑点(现 3);
- 佣金抄目标券商真实条款+缓冲(demo 实测 0, 现 7 为纯悲观缓冲, 保留);
- 纪律:成本只许轻易向悲观改;向乐观改必须 ≥50 笔实测撑腰。

## 3. 对账 metrics 扩展(原步4剩余)

- PF / 最大回撤 一致率(pf_bt vs pf_actual 等)进 metrics JSONB(表不动);
- 按 env 拆 demo/live:reconciliations 加 scope='demo'/'live' 行(只加行不改表)。

## 4. 门禁与自动化(原步5)

- `match_score`(或 q10_pass)进关2门禁:对账达标 + 样本够 + 风险可控才准 DEMO→LIVE;
- 自动重算:有新成交标脏 → 重算对账(现为分析页按需算);
- 接收自 v0.2 §8:实盘评价加权(对账偏离最优先 > live>demo > 时间衰减 > 样本置信 > 风险调整)
  → 综合"实盘评分"或直接做 DEMO→LIVE 准入门禁。

## 5. 阈值进 config(多文档承诺的欠账, 集中还)

- 对账 v1 阈值 0.9(现写死 q10_target)
- 对账最少笔数门槛(样本不够不判定)
- (顺带)v1.1-B 的粗筛线:笔数≥N / 净点>0

## 6. 远期(原 Phase 3)

- reconciliation_history(append-only)追踪"校准后 match 是否变好";
- 按品种覆盖的偏离容忍表(v0.2 §9.3 设计, 多品种实盘后再评估是否需要)。

## 顺序建议

1(SL/TP 回写, 半天) → 5(阈值进config, 半天) → 攒样本 → 2(校准) → 3 → 4(门禁, 配合 v1.1-C 前落地)。
