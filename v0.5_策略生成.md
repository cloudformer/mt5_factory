# v0.5 策略生成 · 已完成部分(归档 2026-07-17)

> 原 `v1.5_策略生成.md` 拆分:本文 = 已实现记录(as-built);未完成部分 → `v1.1_策略生成.md`。

## 已实现

### 生成基础(更早期)
- 模板 × 参数 → 实例:`strategy_core` 三模板(ma_cross / breakout / intraday_multi),
  grid(网格)/ random(随机采样)/ ai(外部生成器协议桩 `ai_generator_url` `/propose`, 配置页可配)三种模式
- 唯一约束 (template, symbol, timeframe, params) 防重复生成;品种必须已登记(向券商校验过)
- 门控模板 intraday_multi:指标搭配 = 参数开关(use_trend/use_atr), 一个模板覆盖整个家族
- 策略名 128→256(修多参数模板生成被拒)

### 漏斗省算力:交叉测试门槛(2026-07-16, schema/021)
原 v1.5-B "2000×9 全跨品种傻跑不行" 的解法落地:
- 批量回测勾"跨品种"时:策略**主品种成绩过 config 门槛**(cross_symbol_gate: 笔数/胜率/净点/PF/回撤,
  每项可空=不查, 全空=全交叉)才展开交叉品种;没过/没测过的只跑主品种,下一批自动够格
- 按 ID 点名 = 点名即信任,不走门槛
- 预览所见即所得:"主品种 N + 交叉 M(够格 x/y)" + 未测过的 ~20% 预估(仅显示)

### 执行基础设施(2026-07-16, 支撑规模化)
- 批量回测 jobs 化(schema/020):投递即返回、SKIP LOCKED 消费、租约回收、**断点续跑**、显式取消;
  多副本安全(铁律5/6),为一键管线(v1.1-B2)铺平
- 下载/回测标准进度条组件(pbar 宏 + 轮询)
