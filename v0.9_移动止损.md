# v0.9 移动止损(trailing stop / 动态 SL)· 设计与实施顺序

> 状态:**第 1~3 步已完成(2026-07-25, 回测侧全链)**;第 4~6 步(runner 实盘/对账留证/授权)待做。
> 已交付:`strategy_core/trailing.py` 纯函数(三类一骨架/零状态棘轮/BUY-SELL镜像)+
> 引擎接线(`_walk_exit` 每收盘 M1 棘轮, tsl/tsl_gap 出场码, keep_tp=false 去TP)+
> v2.4 加 12 个 trailing 用例(36 全绿, 集成用例实证同一笔 开+120 vs 关-100)+
> 配置管道(042 种子 trail_default=null 全关 + jobs/对账重放同一回落规则=统一配置铁律)+
> 分析页·回测分析 tab「四档对比」(内存现算×4 不落库, 探针参数自适应, [用这档]写回 params.trail)。
> **落库×4(批量按档位对比/排名)= 未来触发项**(2026-07-25 与 Frank 定: 先单策略内存×4
> 验证功能, 真需要全库规模对比再上存储 — PK加variant维免维护成立, 代价在计算×4与全部读者加过滤)。

---

## 一、功能讲解

### 解决什么问题

策略经常"快到止盈又跌回来":10 元买涨、SL 9.5、TP 12,冲到 11.5 又跌回 9 —— 本可赚 1.8
结果亏 0.5。移动止损让 **SL 随价格有利移动、只上不下(棘轮)**,锁住到手利润:到 11.8
把 SL 抬到 11.5,最坏带着利润走。**弥补"到手利润守不住"的策略**(如 3677 冲高回落连亏),
不是笼统"回撤大"。

### 作用对象 = 持仓,不是策略(所以天然全适用)

插件只需要一笔仓的四样东西:方向 + 入场价 + SL + TP —— 不关心它是哪个模板/参数/信号开的。
因此 **BUY/SELL 完全镜像**(SELL: `new_sl = min(当前SL, ask参考价+gap)` 只下不上)、
**任何模板/品种/周期/demo/live 通吃**、新模板自动获得能力零适配;唯一前提"这笔有 SL",
而铁律2(每笔必带服务端 SL)保证前提恒成立。**手动单不管**(runner 按 magic 只管自己策略的仓,
不碰你手动开的)。

### 三类 = 同一骨架的参数组合(不是三份代码)

骨架永远一句话:**每根收盘 M1,算"新 SL 应在哪",只往有利方向改单(棘轮)。**
零状态公式(2026-07-25 精化③):**`new_sl = max(当前SL, 最新收盘M1参考价 − gap)`**(BUY;SELL 镜像)
—— SL 只上不下,**当前 SL 本身就编码了全部历史**,不需要"入场以来最高价"这个状态、
不需要回看历史 bar;重启/断线从 MT5 读回当前 SL 即全部所需。

| 类型(active 值) | 新 SL 算法 | 参数 |
|---|---|---|
| **fixed** 固定移动 | SL 跟最高价下方固定距离 | `gap`(点) |
| **breakeven** 保本+移动 | 先到 `start`(如 1R)把 SL 拉入场价,之后同固定移动 | `gap` + `start` |
| **atr** ATR 移动 | 距离 = ATR × 系数(自适应跨品种) | `k`(系数) |

砍掉"档位/阶梯"(手动定档=粗糙版固定移动,连续移动完全覆盖)。

### TP 处理

- **默认 TP 不动**(只移 SL 已解决"守不住利润");
- 可选 `keep_tp=false` 去掉 TP,让盈利彻底奔跑(趋势策略);
- **绝不做"TP 往上移"**(边让跑边设天花板,自相矛盾)。

---

## 二、数据落点(动手前定死)

### 1. 插件配置并进 params —— 不新开策略字段

trail 配置就是几个数值,塞进策略现有的 `params` JSONB。**回测和实盘 runner 读同一行**,
所以时间/策略/货币/手数/插件配置天然统一(不需要"运行时检测",就是读同一份配置)。

```json
"trail": {
  "active": "fixed",           // 当前启用哪个; null/缺失 = 不启用(默认)
  "fixed":    {"gap": 50},
  "breakeven":{"gap": 50, "start": 100},
  "atr":      {"k": 2.0}
}
```

**切换 = 只改 `active` 一个字段**:三类参数都留着,来回切不用重填;`active=null` 或无 trail 键 = 不启用。
"有没有插件"= `trail.active` 是否为空。

### 2. 全局默认 `config.trail_default` —— volume_default 同款

- config 里放 `trail_default`(定默认用哪个 + 各类参数);策略 params 的 trail **覆盖**全局;
- 策略没填 trail → 用全局默认;填了 → 用自己的;
- **按 user 可配**:走"config 全局默认 + 未来按 userid 覆盖"那套(现只有你=全局那行,
  未来每 user 读自己的 trail_default)。

### 3. `trades` 加一列 `trail_used` —— 唯一的新字段(成交留证)

成交回写时记"这笔平仓时生效的 active"(如 "atr"/null)。为什么必须:对账守"统一配置(含插件)"
铁律,回测重放要用**和实盘那笔一样的插件状态**才不判背离;而策略中途可能改过 active,
光靠"策略当前 active"推不准 → 每笔账自带"我被谁管过",严丝合缝。
(SL 被移动过时 close_reason 仍是 "sl",看不出原始还是移动后 → 靠 trail_used 补这个信息。)

---

## 三、当前口径(2026-07-25 定,稳定后再议)

1. **每策略回测必须能切 trail_type**(核心能力,现在就要):回测分析页四选一
   〔关闭 / 固定 / 保本 / ATR〕,内存跑对比;
2. **默认用一个**:`config.trail_default` 定默认;
3. **default 也走 config、按 user 各自配**(volume_default 同构);
4. **稳定后再商议**"是否收成全局一把梭"—— 现在不锁死,保留每策略可覆盖。

---

## 四、五条运行铁律

1. **共享逻辑一份 = 独立纯函数,不放 Strategy 基类**(2026-07-25 精化①):
   `strategy_core/trailing.py` 里一个纯函数 `trail_new_sl(持仓, 最新收盘bar, trail配置)`,
   引擎和 runner 在"有 trail 配置时"直接调 —— **策略类完全不知道 trailing 存在**
   (策略管进场,trailing 管持仓,两层不揉一个类)。不放基类的原因:基类钩子=给每个模板
   留"覆写"口子,将来某模板自定义一版就分裂,违背"基础设施固化";纯函数最好测(v2.4 直接
   喂参数断言)、最符合一份逻辑。**不是单独服务**(单独进程碰同一 MT5=IPC 冲突;
   只在实盘做则回测/实盘分裂尺子废;铁律7 先扩副本后加服务)。
2. **只追赢**:SL 只往有利方向棘轮,只在"更好时"改单,不降低已锁利润。
3. **默认关闭**:现有策略零影响,回测逐字节不变(v2.4 测试保证)。
4. **M1 粒度 + 零状态**(2026-07-25 精化②③):trailing 在**收盘 M1** 上评估
   (不是 TF bar —— M15 策略也用 M1 追):回测端 `_walk_exit` 本就逐根 M1 检查 SL/TP,
   trailing 更新塞同一循环顺手;实盘端 runner 10 秒一拍看最新收盘 M1;锁利窗口从
   15 分钟缩到 1 分钟,仍是 bar 级(M1+, 铁律不破),**两边同一规则=对账天然一致**。
   零状态:`max(当前SL, 参考价−gap)` 幂等,不记忆历史(MT5 当前 SL 即真相)。
5. **韧性 + 券商 SL 永在**:trail 参数随策略进 runner 内存快照;API 挂/worker 失联 →
   用上次快照继续管仓。**棘轮上去的 SL 早挂券商**,授权过期/离线/停更最坏只是"不再上爬",
   绝不撤保护、持仓不裸奔。

## 五、授权(通行证模型,currently open,未来接 license)

授权 = API 拉取时把 `active` 发成实值或 null(受 license/有效期 gate),进 runner 快照。
**只在"加载/拉取"那一刻起作用**:一旦仓在被追就不受它管。三种情况统一表现:

| 情况 | 效果 |
|---|---|
| license 没有 / 过期 | 下次拉取 active=null → **新仓不追**;已开仓 SL 停当前位不撤 |
| 离线 | 用上次快照继续,不管 API |
| 已加载 | 按快照 active 跑,后续 payload 不影响这台 |

**统一规则:license/过期/离线 = "active 拿不到新值或变 null" = "不再追,但不撤保护"。**
runner 只把 SL 往好里改、从不往回改。**目前 active 恒按配置发(全开放)**;未来纳入 license。
**回测不看通行证**(验证永远允许跑)。

---

## 六、三条对比铁律(校准尺子,普适 —— 实现时升 CLAUDE.md 对账口径)

回测 vs 实盘对比时,**除待测变量外全部锁死相等**:

1. **统一时间**:窗口一致;没运行区间就用实盘单边时间(=v1.5 segments/one_sided,已做);
2. **统一配置(含插件)**:两边同一套配置,**包括移动止损开/关**(实盘当时开着,回测重放必须也开;
   靠 `trades.trail_used` 知道实盘那笔的状态);
3. **统一手数**:两边同手数(已做;净点与手数无关)。

**分清两种对比**(同一页面答两问题):
- **对账**(回测 vs 实盘):变量=回测vs实盘,其余全锁**含插件状态与实盘那笔一致** → 答"回测准不准";
- **插件效果**(回测 vs 回测):变量=插件 开vs关,其余全锁 → 答"trailing 增不增收"。

呈现:「人工策略分析·回测分析 tab」加〔关/固定/保本/ATR〕切换,内存跑多版重放并排
(净点/胜率/PF/最大连亏/逐笔),不刷新、不落库(复用 v1.5 内存重放,几乎零成本)。

---

## 七、实施顺序(依赖 → 难度 → 风险;先能回测自证,再上实盘,最后授权)

### 第 1 步 · trailing 纯函数 + 引擎接线(核心先做)
新建 `strategy_core/trailing.py`:纯函数 `trail_new_sl()`(fixed 骨架, 零状态公式);
`_walk_exit` 的 M1 循环里(有 trail 配置时)每根收盘 M1 先算 new_sl 更新 pos["sl"] 再判触发。
依赖:无。难度:**中**(动撮合循环)。风险:**中** —— **v2.4 测试网正为此建**:
改完 `make test-engine` + 加 trailing 用例(棘轮只上不下/保本触发/带trail出场/SELL镜像口径),
全绿才算。默认关→现有策略回测逐字节不变。产出:回测能跑"开 trailing"版。

### 第 2 步 · 参数进 params/config + breakeven/atr 补全
params 支持 trail 结构 + `config.trail_default` 兜底;补 breakeven/atr(同骨架);进参数空间(生成器可撒)。
依赖:1。难度:**低-中**。风险:**低**(纯参数扩展,默认空=不启用)。

### 第 3 步 · 回测分析页 开/关/切类型(自证价值,人能看见)
回测分析 tab 加〔关/固定/保本/ATR〕,内存跑多版并排。依赖:1。难度:**低**(复用 v1.5 重放)。
风险:**低**(纯读不落库)。**做完即可用数据回答"某策略开哪类 trailing 划算",无需上实盘。**

### 第 4 步 · runner 实盘执行(order_modify)
runner 每收盘 bar 持仓中调同一 manage;SL/TP 变好 → MT5 `order_modify`(棘轮/幂等)。
成交回写填 `trades.trail_used`。依赖:1。难度:**中**。风险:**中-高**(碰真金改单)——
先 demo 对账验证"实盘 trailing 与回测重放逐笔一致"再上 live;Windows 侧改动需 update worker。
护栏:券商 SL 永在;只棘轮;重启从 MT5 读持仓重建。

### 第 5 步 · 对账接入"统一配置(含插件)"
对账重放按 `trades.trail_used`(实盘那笔的插件状态)跑,否则开 trailing 的实盘被判背离。
依赖:3、4。难度:**低-中**。风险:**中**(对账口径改,v2.4 对账用例护)。

### 第 6 步 · 授权通行证 + config 加 trail_default 键
payload 带 active(受 gate,目前恒按配置发)进快照;`trades.trail_used` 已在第4步。
未来接 license 有效期。依赖:前面。难度:**低**。风险:**低**(默认全开)。可延后到真要 license 时。

### 顺带
三条对比铁律 → 升 CLAUDE.md 对账口径;v2.4 套件加 trailing 用例(第1步同步)。

### 否决过的替代方案(防再议)
- **MT5 终端自带 Trailing Stop**:终端侧(终端关=停,不比我们强)、只有固定距离一种、
  参数不进系统、**回测完全看不见它** → 对账即废。不用;
- **单独服务**:IPC 冲突/回测分裂/铁律7,见铁律1;
- **Strategy 基类钩子**:见铁律1(精化①,改为独立纯函数)。

---

## 七点五、已知残差与边界(实事求是, 不假装 100 分)

1. **改单摩擦不建模**:实盘 `order_modify` 可能被券商拒(SL 太贴市价, 触 STOPS_LEVEL/
   FREEZE_LEVEL 最小距离)→ runner 处理 = 失败即放弃本拍、下拍重算重试(幂等, 棘轮不倒退);
   回测不建模这层摩擦 —— 接受为已知残差, 与入场滑点残差同级, 对账的净点差会如实暴露它;
2. **参数与券商约束校验**:`gap` 小于券商最小停止距离时改单永远被拒 → 生成/校验时
   `gap ≥ 该品种最小距离` 进 valid 检查, runner 端也容错(拒了就记日志不重试轰炸);
3. **SELL 镜像口径**:SELL 的参考价用 ask(bid+当根点差), 与 `_walk_exit` 现有 bid/ask
   口径逐行对齐(v2.4 用例单独覆盖 SELL);
4. **trail_used 的定义钉死**:= runner 对这笔**实际使用**的 active(开仓时快照值),
   平仓回写时记录 —— 不是"策略当前配置"(中途改配置不影响已开仓的记录口径);
5. **最后几分只能靠数据**:设计正确 ≠ 有收益 —— gap 太紧被噪声扫出(whipsaw)、太松锁不住,
   到底增不增收是经验问题, 第 3 步(开/关对比)做完由真实回测数据回答, 文档给不了。

---

## 八、一句话总览

**先在回测里做出 trailing 并能开关/切类型对比(1→2→3,自证价值,零真金风险),
再上实盘执行(4,demo 对账验证后才 live),最后接对账口径与授权(5→6)。**
每块都落在现有架构既有能力上(共享策略层/内存重放/快照缓存/券商SL/漏斗调参/config默认+覆盖),
无新服务、无破例。稳定后即基础设施,和撮合引擎同级,不需一直调试;"调"的是每策略 trail 参数,
交给漏斗(生成撒维度→回测打分→OOS/对账筛),非手动伺候。
