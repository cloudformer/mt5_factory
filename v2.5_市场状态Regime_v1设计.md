# v2.5 市场状态 Regime v1 · 设计(2026-07-27 与 Frank 定, 只讨论未开工)

> 定位: 第二阶段主线 — 回测准确率已达标(v2.4 锁死), 下一步不是继续提升回测,
> 而是提升 **Strategy Selection**。本文是 Regime v1 的完整设计记录;
> 将来验证出第四维度(成交量/流动性/时段)再立 Regime v2, 演进全程留档。

## 核心思想(Frank 原设)

**没有任何一个策略能长期适用于所有市场**(已被自己的数据实证两次:
通用 trailing 档不存在、177 样本内幻觉)。真正的问题不是"哪个策略最好", 而是:

1. 当前市场处于什么状态(Market Regime)?
2. 哪些策略最适合当前这个状态?

**核心研究假设**: 策略真正适应的是 Regime, 而不是某个货币对。
若 EURUSD 的 AAA 格里表现好的策略, 在 GBPUSD/AUDUSD 的 AAA 格里也接近 →
Regime 比品种更重要; 不成立 → 品种仍是必要维度。**由数据裁决, 不预设答案。**

## Regime v1 定义: 三维八格(最小变量集)

| 维度 | 状态 | 口径(常量, 开工前可改一次, 之后**冻结永不优化**) |
|---|---|---|
| Long Trend | A=Bull / B=Bear | D1 收盘 vs SMA200 |
| Short Trend | A=Bull / B=Bear | D1 收盘 vs SMA20 |
| Volatility | A=High / B=Low | D1 ATR14 vs 其过去一年(252根)中位数 |

组合 = AAA…BBB 共 8 格。**设计原则(Frank)**: 尽可能最少变量;
每增加一个变量必须证明它显著提升分类能力, 否则不进(Minimal Effective Feature Set)。
演进路径: Long → +Short → +Vol → (+Volume, 仅当数据证明需要)。

### 两条铁纪律

1. **口径冻结**: 200/20/14/252 是常量不是参数 — regime 定义若可调优 = 递归过拟合,
   整套矩阵立刻失去意义;
2. **无未来函数**: 某日的交易只能用**截至前一日收盘**算出的 regime 标注;
   粒度 = 日级(每天一格), 不做 per-bar(v1 没必要, 徒增噪音)。

## 口径评定标准(地基第一步, 2026-07-27 与 Frank 定)

> 推进顺序(Frank 定): 先不加表, 从地基做起 — ①口径评好(有了标准所有货币对才出 regime)
> → ②货币对有 timeline → ③才轮到策略矩阵。
> **定标准的方法与回测同源**: 下面的门槛数值只是"初始参考值" —
> 先把记分卡在真实数据(2015~今, 全部下载品种的 D1)上跑出来, 看到真实数字,
> 再校准门槛(允许校一次), 然后**连口径带门槛一并冻结**。

### 四条标准(全部中性指标 — 禁止用策略盈利选口径, 那是用答案选尺子)

| # | 标准 | 本质 | 指标 | 初始参考门槛(待记分卡校准) |
|---|---|---|---|---|
| 1 | **持续性** | 格龄必须显著长于策略持仓(现役策略持仓几小时~2天), 否则一笔没走完天气就变, 归因失义 — **格龄/持仓比是灵魂** | 每维度中位格龄(dwell, 交易日) · 组合格中位格龄 · 年组合翻转次数 | 长趋势 ≥20d · 短趋势/波动 ≥5d · 组合格 ≥3d · 年翻转 ≤60 次; 坏: 组合格 1~2d(噪音开关) |
| 2 | **覆盖均衡** | 稀到攒不出统计的格子等于白设 | 全期每格天数占比(逐品种) | 最稀格 ≥5%(十年≥130 交易日) · 最密格 ≤35% · 八格全非空; 坏: <2% 或 >50% |
| 3 | **区分度(同期性格, 非预测)** | 标签是否对应真实的**同期**市场性格差异(不是能否预测未来) | 高波格/低波格 同期日均真实波幅比 · 长趋势 A/B 两态**同期日收益**均值差(t 值) | 波幅比 ≥1.5× · \|t\|≥2; 坏: 各格分布重合 |
| 4 | **维度不冗余** | 两维若高度同向, 8 格塌成 4 格("两个值一样就是一个值") | 任意两维同向一致率 | ≤75%; ≥90% = 冗余砍一维。**兼作未来收第四维度的门槛** |

- **格龄指标定义修订(2026-07-29, XAUUSD 20年实证)**: 中位格龄一律用**按天加权中位**
  ("随便挑一天, 它所在段有多长"的中位 = 段长的加权中位, 权重=段长)。按段数的朴素中位
  被穿越期碎段淹没 — 实证: SMA200 长牛大段动辄数百天, 但穿越抖动制造大量 2~3 天碎段,
  按段中位只剩 5 天完全失真(按天加权后为数百天)。旧按段值在记分卡括注对照一段时间后退役;
  门槛数值(长≥20 等)暂沿用, 待多品种记分卡后一并校准;
- **③尺子收口(2026-07-29 与 Frank 定, 基础性纠偏)**: regime 只描述"今天什么天气",
  **绝不预测明天赚不赚**(那是策略的事, 有效市场也测不出)。曾把③做成"次日/前瞻收益 t 值"
  = 逼 regime 做预测, 全线测不出(|t|≈0.5~0.8, 8口径×10品种×2粒度铁一样地不过), 然后被迫
  想"换维度/加维度"— 这是拿错尺子导致的"不知目的地的乱导航", 已废弃。
  改为**同期性格描述**: 波动=高波格当天真实波幅确实更大; 趋势=牛态那些天市场当天确实在涨。
  标签"名副其实"即可, 零前瞻。**regime 的价值不在预测, 在于给策略分环境看表现**;
- **标准与门槛全系统通用, 不按品种分**(2026-07-29 与 Frank 定): 三维定义全部相对自身
  (价格 vs 自己的均线 / ATR vs 自己的分位), 口径天然每币自适应; 按品种开小灶会让
  跨品种同格对照失义(核心假设检验瓦解)。评定看整体(中位+最差), 单品种单项贴线不否决;
- 标准 2 预警: 覆盖受市场本身摆布(如 XAUUSD 长牛 → 长趋势 B 格天然稀) —
  判"稀到无用"而非强求均匀; 稀格正是"跨品种合并统计"假设的用武之地;
- 时间口径: 格龄单位=交易日(FX 每周 5 天, ≈1.4 个自然日);
- **标准④若不过的替换后手**(2026-07-27 Frank 问"长短线会不会天生冲突"时定):
  长短趋势语义上不冲突 — 不同向的 AB=牛市回调、BA=熊市反弹恰是最有信息的格子;
  但两维共享 close 且窗口嵌套, 天生有同向倾向, ④就是它的探测器。
  一致率 ≥90% 时把短趋势换成不共享 close 的定义: `SMA20 > SMA50`(金叉式)或 SMA20 斜率。

### 落地顺序修订(2026-07-27 Frank 定: 建表先行, 页面即记分卡)

timeline 表与 Regime 页面**先建**(用默认候选 C1: SMA200 / SMA20 / ATR14-年中位),
页面直接展示演变与四标准统计 — 看真实数字判口径, 取代离线脚本跑记分卡。
**口径仍未冻结**: 表极小(一品种一天一行), 换口径 = 改常量一键重建(秒级), 成本可忽略;
评定完成后把选定口径与门槛写回本文档并冻结。

### 两层参考分(2026-07-28 与 Frank 定 — 关键规则, 仅参考不做门禁)

核心哲学: **假的东西谈不上好坏** — 先验真伪(及格), 及格了才谈操作品质(好坏)。

**及格层 = ③区分度(60 分)** — 回答"这套标签是不是真的":

| 子项 | 分值 | 打分 |
|---|---|---|
| 高/低波幅比(门槛 1.5×) | 30 | 实测/1.5, 封顶 1 × 30 |
| 趋势 t 值(门槛 2) | 30 | \|t\|/2, 封顶 1 × 30 |

**好坏层(40 分)** — 及格了才谈操作品质, 按原重要性缩排:

| 标准 | 分值 | 子项 |
|---|---|---|
| ①持续性 | 20 | 5 个子项各 4 分(长/短/波格龄、组合格龄、年翻转) |
| ②覆盖均衡 | 12 | 稀格 6 + 密格 6 |
| ④不冗余 | 8 | 一致率 1 项 |

**合成规则(2026-07-29 修订: 两层直接相加, 细则未来再探讨)**:
- 总分 = 及格层 + 好坏层 **直接相加**(满分 100) — 真伪层权重大(60)即体现重要性;
- ③门槛过没过只作**标注**(徽章颜色/③未过线字样), 不再压分(原"未及格好坏暂不计"闸门作废);
- ③未算 → **不出总分**(没验真伪就没有分, 只给好坏层预览);
- 样本 <252 交易日 → "未证实"压顶, 分数仅供机制调试。

所有子项为**连续打分**(实测/门槛, 封顶 1) — 比 ✅❌ 多一层"差多少"的分辨率;
四标准 ✅❌ 判定与样本量纪律仍是正式结论的依据, 参考分不做任何门禁。
实现: web 视图层现算(views/datasync._score 逻辑), Regime 页总评行显示, 悬停看分项。

### 已实现清单(2026-07-28 · 阶段一"看得见"交付, 等真数据)

**推进纪律**: 先精确格、验证准确后再谈汇总 — 通配(AA?/A?A)与邻域合计推后, 此前不增加噪音。
**共同性质**: 归因全部读时现拼 — 交易/回测/对账(reconciliations)落库零改动,
改口径重建时间线后所有页面自动换新标签(零垃圾零重跑), 整体撤除 = drop 一张表 + 删显示代码。

基础设施:
- `regime_timeline` 表(schema/047): 三维各一列 CHAR(1) + regime 生成列; 口径配置(schema/048,
  config.regime_params, 配置页可改带边界校验 + 档位速查注释); services/regime.py:
  M1→D1 券商日聚合、无未来函数(右移一日)、UPSERT 覆盖 + 头部修剪、读时自愈(仅日期过期);
- Regime 页: 顶部货币对选择器 → 近一年色带(按季分行) → 当前状态横条 → 2×4 八格象限
  (权重即布局, 色阶=AAA绿→BBB红八级渐变, 唯一定义在 _macros) → 口径记分卡
  (评定流程①改口径②重建③看分 + 四标准 + 两层参考分) → 演变明细(前端切片取最近N天);
- 区分度(标准③)按当前配置现算(full=1 手动点, 重扫 M1), 与①②④"上次重建口径"的先后关系已钉死。

策略归因(人工分析页):
- 三处逐笔明细贴入场日格子(回测归因 / 实盘归因 / 移动止损对比某档明细),
  各用**自己回测品种**的时间线(实盘/对账用主品种), 时间线未覆盖显 —;
- 三处八格汇总(同一张 2×4 象限图): 回测=笔/胜率/净点/PF(<20 笔灰"未证实"),
  实盘=同款, 对账=每格 实盘/回测 各自笔数+胜率(同格对照"回测的赢法实盘还成立吗");
- 对账逐笔对照表加 Regime 列(配对/缺口行都贴, 缺口归因是否与格子相关一眼可查)。

排名页 R1/R2/R3 三列(2026-07-28 Frank 定, 实施版):
- 每策略胜率前三的**合格格**(主品种回测逐笔 × 时间线, 一条 SQL 当页现算,
  `HAVING count(*)>=20` 在库里执法未证实纪律), 显示 `格子 胜率%(笔数)`;
- 排不满的槽位如实报原因: 先 `—(不够20笔)`(有格子有交易但未证实, 每个未证实格占一槽),
  再 `—(无交易)`; 只展示不参与服务端排序(按格排序全库需物化 = 阶段三事项);
- 掩码筛选愿景(通配落地时顺承): 三维独立列使 `A??`=长趋势牛, `?B?`=短趋势弱, `??A`=高波
  直接是 SQL 条件 — 按天气筛策略的查询语言, 数据结构已就位。

**当前状态**: 全链路管道已通(合成数据 E2E 验证), 但**口径是调试档 = 假标签**,
所有格子数字只看管道不作结论(实测长相: 格间胜率挤在一起、分布无结构 — 假天气的特征);
下一步 = 长历史下载 → 正式档口径评定冻结 → 重建时间线, 全站归因自动换真。

### 数据源双层 M1+D1(2026-07-29 与 Frank 定)

MetaQuotes-Demo 历史深度实测(XAUUSD 逐周期探针, "越细越短"金字塔):

| 周期 | D1 | H1 | M30 | M15 | M5 | M1 |
|---|---|---|---|---|---|---|
| 深度 | 16年+ | 10年+ | 4年+ | 2年+ | <14个月 | ~4个月 |

(前置排障: 终端"最大柱数"默认 ~128k 根曾把全品种掐在整齐的 ~12万根 — 根数对齐而非
日期对齐是它的指纹; 已解锁 20M + terminal_start.ini 固化, 但解锁后 M1 依然只有 4 个月 = 真墙)

- **两层下载**: M1(唯一原始数据 — 回测/对账/聚合的原料) + 原生 D1(regime 长视野补头,
  CLAUDE.md 预留例外正式启用); 周期层进配置(config.download_timeframes, schema/049,
  配置页可见可改, M1 锁死必含), 管道 timeframe 参数化;
- **中间层(H1/M15)不下**: 回测撮合悲观规则依赖 M1 拆 bar(SL/TP 同 bar 顺序), 原生高周期
  跑的回测段与 M1 段是两把尺子, 成绩不可比 — 回测要长历史的正解是 v2.3 接 M1 深的券商;
- **regime 双源合并**(services/regime._d1): 有 M1 的日子用 M1 聚合(与交易同源),
  更早头部用原生 D1 行补, 重叠日 M1 优先 — 区分度③现算走同一函数自动受益;
  D1 下载完成后需手动点重建(读时自愈只盯 M1 日期);
- 口径评定期与格龄单位不变: 全部日级统计, D1 补头只是把可评定窗口从 4 个月拉到 20 年。

### "建议 space"标准(推后, 与通配一起等 regime 验真后做)

与选拔规则"本格达标+邻格不崩"同构:
- 格子合格 = 笔数 ≥20(未证实纪律) 且 净点 >0;
- 建议 space = 净点最高的合格格;
- 推荐等级: ★★ = 本格合格 **且** 3 个海明邻格中 ≥2 个不崩(净点 ≥0 且样本 ≥20);
  ★ = 本格合格但邻居撑不起; 无合格格 = 不给建议;
- 未证实邻格不算"好邻居"也不算"崩" — 没证据不投票, 凑不满 2 票就到不了 ★★。

### 评定流程与产出物

1. 候选 ≤8 组(2×2×2): 长趋势 {SMA200 / EMA200} × 短趋势 {SMA20 / SMA50} ×
   波动 {ATR14 vs 过去252日中位(高低各半) / ATR14 vs 67分位(高波约占1/3)};
2. 每候选 × 每品种跑一遍 → **记分卡**(要生成的"图"):
   - 四标准原始值汇总表(候选 × 品种, 取中位 + 最差品种);
   - 每格占比明细(逐品种一行, 8 格各占多少);
   - regime timeline 示意(抽一两个品种画格子随时间的分布带, 肉眼核对"像不像天气");
3. 对着真实数字给候选打分 → 选定一组 → 校准门槛(一次) → **冻结**, 写回本文档;
4. 全程只读(D1 从 M1 现场聚合), 不写库不建表 — timeline 表等口径冻结后才建。

**工具已上线(2026-07-29)**: Regime 页记分卡区「候选族对比 →」= `/regime/evaluate`,
8 候选 × 全部下载品种现算(内存只读, 不碰 timeline): 主表=两层参考分矩阵(每候选
中位/最差, 按中位降序), 副表=四标准原始值每候选跨品种中位(最差) — 门槛校准的主工具。
历史不足暖机的品种如实跳过。首份真数据发现(XAUUSD 20年, SMA200/20/q0.5):
及格层 34/60 — 波幅比 1.5× 踩线, 趋势 t=0.3(长趋势状态对次日收益无区分力) —
待多品种数据齐后判定: 换候选能否救, 还是 t 指标该量"整段持有收益"而非次日。

## 架构(按系统铁律, 新表只建一张)

- **`regime_timeline(symbol, date, regime)`** 主键(symbol,date):
  从 D1(M1 聚合派生)幂等重算, UPSERT 覆盖, 免维护 — 独立计算, 不依赖任何策略,
  全部策略共用同一条 timeline;
- **矩阵第一版现算不建表**: `backtests.trades` 已存逐笔(含 entry_time) —
  Regime 归因是**纯事后统计**(JOIN timeline + GROUP BY regime), 回测引擎零改动、
  存量 2570 份成绩单零重跑。StrategyRegimeMetrics 真列表(strategy_id, regime, pf,
  win_rate, sharpe, max_dd, expectancy, trade_count, score; 主键 strategy_id+regime)
  **等 A/B 验证赢了再建**(Frank 自己的原则: 每个新增都要先证明值得);
- **跨品种假设有现成数据**: cross_symbol 回测的多品种成绩已在库 —
  矩阵按 (strategy, symbol, regime) 算, 同 regime 跨品种对照即出答案。

## 选拔与下架规则(海明距离, 2026-07-27 Frank 定)

环境是渐变的, 一次通常只翻一位 — 距离本身就是滞后机制, 不需要额外防抖:

| 当前 regime 与选拔时的海明距离 | 动作 |
|---|---|
| 0(同格) | 正常持有 |
| 1(一位之差) | **持有观察** — 选拔时已要求邻格不崩, 渐变可容忍 |
| ≥2(两位以上翻转) | **下架** |

**选拔条件**: 本格分数达标 **且 三个距离=1 的邻格不崩** —
与漏斗既有的"参数邻域稳健性"同构: 参数空间防尖峰, regime 空间防环境突变。

## 统计纪律(最大的坑)

- **每格最少笔数 ≥20**(默认, 可议): 8 格切分后, 30 笔的策略每格平均不到 4 笔 =
  纯噪音。格内笔数不足 → 该格标"**未证实**", 不给分不参与选拔 —
  与全局纪律一致(小样本=未证实, 不是已证明);
- **格分布天然不均**: 长牛市里 B?? 格样本稀少 — 稀格的结论永远晚到, 接受它, 不硬凑。

## 验证路径(三阶段, 每阶段过关才进下一阶)

1. **阶段一 · 看得见**: 建 timeline 表 + 现算矩阵 + 页面展示(某策略的 8 格成绩 /
   当前各品种处于哪格) — 只读, 零行为变化;
2. **阶段二 · 赢一场 A/B(成功判据)**: 同一段留出期, 对比三铁律下 —
   「按当期 regime 选各格 top 的组合」 vs 「按总分选 top 的组合」比净点/回撤。
   **赢了才有阶段三**; 输了记录结论, regime 降级为分析维度;
3. **阶段三 · 才谈**: StrategyRegimeMetrics 建表、自动选拔/下架接入执行层、
   第四维度评估(同样要先赢自己的 A/B)。

## 不做清单

- 不做 per-bar regime(v1 日级足够);
- v1 不做自动切换执行 — regime 是**选拔维度不是执行开关**(一按钮一职),
  自动化在阶段三凭 A/B 战绩说话;
- 不堆 feature、不做几十维打分、不上机器学习黑箱 — 8 格可解释、可质询、可复算。

## Success Criteria(Frank 原设, 量化锚点 = 阶段二 A/B)

1. 三个变量是否足够描述大多数 Market Regime?
2. Regime 能否显著提升 Strategy Selection?(A/B 裁决)
3. 能否降低对 Currency Pair 的依赖?(跨品种同格对照)
4. 能否提高 Live 的稳定性?
5. 能否形成可持续学习的 Strategy Knowledge Base?
   (Market Regime → Strategy Capability Map → Selection → Validation → Learning 闭环)

## 进度(2026-07-28 告一段落, 等数据)

| # | 步骤 | 状态 |
|---|---|---|
| 1~5 | 口径配置 / 时间线表 / Regime 页 / 记分卡+两层参考分 / 回测归因八格 | ✅ |
| 5.5 | 归因铺满人工分析页(tsl明细+对账+实盘, 逐笔+八格) | ✅ |
| 4号 | 排名页 R1/R2/R3 前三合格格(槽位如实报 不够20笔/无交易) | ✅ |
| 6 | 通配掩码(A??/?B?) + 邻域合计 + 建议space | ⬜ 推后 — regime 验真后做, 此前不加噪音 |
| 7 | **长历史下载**(Frank 操作) | ⬜ 当前瓶颈, 一切结论等它 |
| 8 | 口径正式评定与冻结(正式档位族, 门槛校准一次, 写回本文档) | ⬜ 依赖 7 |
| 9 | 阶段二 A/B(按 regime 选 vs 按总分选, 留出期对比) | ⬜ 依赖 8 |
| 10 | 矩阵物化 + 自动选拔/下架 | ⬜ 冻结 — A/B 赢了才谈 |
