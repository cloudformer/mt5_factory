# v0.10 持仓时限(时间止损 + 周末平仓)· 设计

> 状态:**规划,待 Frank 过目后动代码**(2026-07-25)。
> 第二个通用持仓管理插件 — SL/TP/trailing 是"价格出场",本插件是"时间出场",经典互补,
> 与 v0.9 trailing 正交可叠加。全套复用 v0.9 方法论(共享纯函数/默认关/对比页现算/OOS 裁决)。

## 一、解决什么问题(自家数据已喊过两次)

- **僵尸仓**:3677 一笔仓扛数天,把整个 7 月的信号堵死 —— 资金占着、机会全错过;
- **周末跳空**:3533 那笔 07-17 20:45 扛周末,周一跳空结局翻转 —— 两天不可控风险白扛。

## 二、功能(两个开关,一个骨架)

```json
"hold": {
  "max_bars": 96,        // 持仓超过 N 根(策略周期的bar)未出场 → 平仓; 0/缺失=不限
  "weekend_flat": true,  // 周五 cut_hour 后的第一个收盘M1 平掉一切, 不扛周末
  "cut_hour": 21         // 周五平仓时刻(券商服务器小时, 默认21; 仅 weekend_flat=true 时有意义)
}
```

- **判断全部无状态**:只需 entry_time(仓自带)+ 当前 M1 时间(券商服务器时间, 铁律口径)——
  连棘轮都没有,比 trailing 还简单;
- 时限换算:`已持仓秒数 ≥ max_bars × TF_SECONDS[策略周期]` → 到点;
- 周末:M1 时间戳的 weekday=周五 且 hour ≥ cut_hour → 平(周一跳空自然规避);
- **出场执行悲观**:按下一根 M1 开盘价成交(BUY 出 bid 开盘,SELL 出 ask=开盘+当根点差,
  与跳空/撮合现有口径逐字一致);
- **出场码**:`time`(时限到)/ `weekend`(周五平仓)——归因与对比区分得开;
- **作用对象=持仓**:任何模板/方向/品种/周期通吃,手动单不管(同 v0.9);
- **与 trailing 叠加顺序**(同一 M1 循环):先判价格出场(SL/TP)→ 未出判时间条件 → 未平做 trailing 更新。

## 三、数据落点(照抄 v0.9 模式)

1. 配置并进 **params.hold**(不新开字段);"有没有插件"= params 里有没有 hold 键;
2. **config.hold_default** 全局默认(schema 种子 null=全关,volume/trail_default 同构,
   jobs 与对账重放同一回落规则 = 统一配置铁律自动成立);
3. 成交留证 `trades.hold_used` —— 随第 4 步(runner 实盘)一起,目前暂停不做。

## 四、实施顺序(1~3 回测侧,4~6 暂停同 v0.9)

1. **纯函数 + 引擎接线**:`strategy_core/holding.py`(`hold_should_close(entry_time, now, cfg, tf) → reason|None`)
   + `_walk_exit` 循环接线 + **v2.4 用例**(到点平/未到不平/周五边界/cut_hour 边界/
   周一跳空对照/与 trailing 叠加顺序/无配置=旧行为逐字节一致);
2. **配置管道**:schema 种子 hold_default=null + CONFIG_KEYS + jobs/对账回落;
3. **对比页**:与移动止损同区,变体 = 关闭 / 仅时限 / 仅周末 / 两者,max_bars 与 cut_hour
   手填(空=默认 96 / 21),内存现算 + 每档明细 —— **评判位=OOS 留出段**(尺子纪律);
4~6. runner 实盘 order_close / trades.hold_used / 授权 —— **暂停**(先回测调好, 同 v0.9)。

## 五、已知边界

- 周五 cut_hour 用券商服务器时间(与 K 线同口径,永不转换);不同券商收盘时刻略异 →
  cut_hour 是参数不写死,默认 21 留余量;
- max_bars 平仓可能恰逢点差最宽的深夜 → 悲观按实际开盘价+点差成交,回测如实计入
  (v7.1 深夜残差同级);
- 假期(非周末的休市日)不识别 —— 第一版只治周末(数据里 95% 的跳空风险),假期日历
  是维护负担,不做(简单易维护)。

## 六、预期验证(部署后第一批实验)

拿 3533(有周末翻转案例)和 3677(僵尸仓案例)各跑:关 vs 仅周末 vs 仅时限 vs 两者,
看留出段 —— 预期 weekend_flat 对"入场在周四五的策略"改善最明显;结论同 v0.9 纪律:
样本不足只出"未证实",数据攒够再定案。
