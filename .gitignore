# Python
__pycache__/
*.py[cod]
.pytest_cache/
venv/

# 数据库数据 (在 repo 内但不入 git)
containers/postgres/data/

# 环境变量 (模板 .example 除外)
env/*.env
!env/*.env.example
.env

# Claude Code
.claude/settings.local.json

# IDE / OS
.vscode/
.idea/
.DS_Store
# worker 运行时状态 (绝不入库: restart.flag 若被提交, 每台 worker 一启动就无限重启)
windows_mt5/runner_status.json
windows_mt5/selftest_result.json
windows_mt5/restart.flag
windows_mt5/update_log.txt
ngrok/ngrok.yml
