# v1.1 小功能清单(一个功能一个功能做)

> 环境:本地 = UI + api + 数据库(**无 worker、无 M1 数据**);Linux 那台有数据;worker 相关等有 worker。
> 分三组:**0 = 已完成;1 = 现在可以做(本地);2 = 需要数据/worker 才能测或做**。每组内按顺序,一次只做一个。

---

## 0. 已完成 ✓

### 0.1 下单备注维度定稿(MT5 流水显示)— 代码完成
- 维度:机器(worker·demo/live 徽章)/ 归属(策略名·下单测试)/ 动作(开仓·平仓)/ 方向(买·卖)/ 原因(**平仓才显示**:止损·止盈·测试)。
- 剩余:worker 在线后肉眼核对(见 2.4)。

### 0.2 手动在 MT5 改 SL/TP 的影响 — 已答复
- **不影响任何数据读写/系统机制**(流水照拉、战绩照算、runner 照跑)。
- **唯一影响 = 策略质量评价**:那笔仍按 magic 记到策略头上 → 战绩掺水、demo 对账对不上。
- 纪律:demo/live **别手动动策略的仓**;v1.2 逐笔落库后可按 reason=手动 剔除。

### 0.3 回测大改 — 本地验证通过(2026-07-13)
- `make up` 自检 **ALL PASS**(含此前 500 的 `/backtests/` → 200)。
- schema 落位确认:009 唯一约束 = `(strategy_id, symbol)`、symbol/broker 列在;010 role 列已删;008 修复版幂等重跑无害(不再毁数据)。
- 剩余:写入路径要有 M1 数据才走得到 → Linux 收尾(见 2.0)。

### 0.4 批量回测「范围」选项(全部 / 仅未测试)— 代码完成
- 筛选行下拉「范围」:默认**全部**(现状不变);选**仅未测试** = 只跑主品种还没有回测记录的策略(`NOT EXISTS`),补漏不重复跑。
- 剩余:有数据环境跑一次确认(随 2.0)。

### 0.5 批量回测对话框专业化 — 完成并本地验证(2026-07-13)
- 布局:**选策略(按筛选/按ID 单选互斥, 选一边另一边灰掉)→ 选项(跨品种)→ 预览+启动**。
- 预览行:实时显示"将回测 N 个策略 × M 品种 = K 次"(新增只读接口 `/backtest/plan`),本地验证:全部=200×1、跨品种=200×9=1800、按ID=1×1 ✓。
- 成本去重:运行表单去掉三框,只读一行显示系统默认(唯一修改处 = 上方"默认成本"区块);去掉"上限"输入与底部长说明。

### 0.6 结果排名多条件过滤 — 完成并本地验证(2026-07-13)
- 过滤项(全部服务端查库,空=不限):最少交易数、**胜率≥%、PF≥(PF=∞视为通过)、最大回撤≤、健壮性≥%(没跑跨品种=不通过)、净点[全部/仅盈利]**。
- 本地验证:每个参数单独+组合调用全 200;真数据语义随 2.0 在 Linux 复核。

---

## 1. 现在可以做 ✅(本地即可开发)

### 1.1 结果排名:规则排序(维度定义,待确认)
| 规则 | 排序依据 |
|------|---------|
| 盈利 | 净点数 ↓ |
| 稳定 | PF ↓ |
| 回撤小 | 最大回撤 ↑ |
| 风险调整 | 净点/最大回撤 ↓(每单位回撤赚多少,类 Calmar) |
| 健壮 | 跨品种盈利比例 ↓ |
- 待确认:5 个维度定义;过滤/排序走**服务端查库**(全量)还是本页即时(≤200条)。

---

## 2. 需要数据 / worker 才能测或做 ❌

### 2.0 Linux 收尾(有数据即可,不需要 worker)【待测试】
1. `git pull` + `make up`(自动执行 008修复/009/010,自检应全绿)。
2. 回测页:**货币对=全部 → 启动** → 补回被旧 008 删掉的 154 条主品种行(含 ID 13),排名恢复。
3. 可选:再跑一发勾**跨品种**的 → 验健壮性列/展开明细真数据;顺手试「范围=仅未测试」。

### 2.1 MT5 流水页头:账户信息卡【待做】
- 加 余额(worker名)/净值/可用保证金/保证金水平(爆仓风险) 四张卡,复用 Demo/Live 页组件,数据来自选中 worker。

### 2.2 Demo/Live 页:worker pool 资金汇总【待做】
- 该角色**所有已指派 worker** 的 余额/净值/可用保证金 **求和**显示一行总计。

### 2.3 多券商下载路由【规划,接第二个券商前必须做】
- 问题:多券商时同一品种会被不同券商数据互相覆盖(historical_bars 无券商维度)。
- 方案:下载按 `symbols.broker` 路由——只让 **server == 该品种 broker** 的 worker 下载;匹配不到跳过+明确提示。

### 2.4 MT5 流水显示核对【待测试】
- 0.1 的显示逻辑(动作/方向/原因/徽章)worker 在线后肉眼核对一眼。
