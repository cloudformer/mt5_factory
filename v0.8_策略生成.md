# v0.8 策略生成 · 验尺实验矩阵(2026-07-27 与 Frank 定, 内功第一课)

> 目的不是直接找赚钱策略, 是**按流派分组对照, 看尺子对每个流派说什么**:
> 哪个流派在哪个品种/周期上还有肉 — 每个结论都由自己的回测量出, 不引用市面传说。
> 模板家底: `strategy_core/templates/` 三个 — ma_cross(双均线) / breakout(唐奇安通道) /
> intraday_multi(慢均线趋势过滤 + RSI 入场 + ATR 闸)。

## 实验总表(生成页逐行执行, 共 8 批 750 个)

| 流派(市面对应) | 组 | 模板 | 品种 | 周期 | 随机数量 | **批次标签**(生成页填, 写进 basis) | 事后怎么看这组 |
|---|---|---|---|---|---|---|---|
| 1 趋势跟踪(海龟/慢趋势) | A | breakout | XAUUSD | H1 | 50 | `实验组A-趋势跟踪海龟-金H1` | channel≥55 是本流派本尊 |
| 1 趋势跟踪 | A | breakout | XAUUSD | H4 | 50 | `实验组A-趋势跟踪海龟-金H4` | **笔数门槛放宽到 10**(高周期天然低频) |
| 2 通道突破(快突破) | B | breakout | XAUUSD | M15 | 100 | `实验组B-通道突破-金M15` | 与 A 同模板不同周期 — 对比"突破在快慢周期谁活" |
| 2 通道突破 | B | breakout | GBPUSD | M15 | 100 | `实验组B-通道突破-镑M15` | GBPUSD 假突破多, 看尺子怎么判它 |
| 3 动量回调(趋势过滤+RSI) | C | intraday_multi | XAUUSD | M15 | 100 | `实验组C-动量回调-金M15` | 存活者里 **use_trend=1 算 C 组** |
| 3 动量回调 | C | intraday_multi | EURJPY | M15 | 100 | `实验组C-动量回调-日M15` | 同上 |
| 4 均值回归(RSI 反转) | D | intraday_multi | EURUSD | M15 | 100 | `实验组D-均值回归-欧M15` | 存活者里 **use_trend=0 算 D 组**(EURUSD 点差最小才养得起高胜率小盈亏比) |
| 对照基准(非新流派) | E | ma_cross | XAUUSD | M15 | 150 | `实验组E-对照双均线-金M15` | 已知有活口的族当标尺: E活A~D死=流派问题; E也死=本批口径有问题 |

**标签命名法**: `实验组X-市面流派名-品种周期` — 稳定前缀保证整组召回。
**按批查找**: 排名页搜索下拉选「标签/生因(模糊)」— 搜 `实验组`=全实验750个,
搜 `实验组A`=单组, 搜 `趋势跟踪`=按市面名称找; 组内再叠 oos_pass 等筛选 =
该组存活名单(存活率的分子)。C/D 用同一模板混采 use_trend, 标签标"撒的意图",
判读时 C 只认 use_trend=1、D 只认 use_trend=0。

**不生成的两个流派(记录原因, 防止将来忘了为什么没有):**

- 5 时段策略(伦敦开盘突破 ORB): 模板缺"时间过滤"维度, **测不了** — 最值得补的模板欠账,
  按"一次一个模块"纪律, 现有三模板跑到极限、看清缺口后再加;
- 6 网格/马丁: **永不做** — 无止损扛单直接违反"每单必带券商侧 SL/TP"铁律,
  其爆仓尾部正是本系统结构性排除的东西。

## 生成方法(细节)

1. 生成页选 模板 × 品种 × 周期, 随机模式填数量, 点生成 — 按总表逐行 8 次;
2. **随机撒的是全参数空间**(RANDOM_SPACE), UI 不支持限定子空间(如 channel≈55 /
   use_trend=1) — 不需要: A 组 channel 自然散布 10~200, 谁活谁死回测说了算;
   C/D 靠事后按 params.use_trend 分组。要精准点名参数时走 AI 页第 2 步粘 combos;
3. 生成即入库(CANDIDATE, 唯一约束防重), 撞已有组合会报"已存在"属正常。

## 回测与筛选口径(对比三铁律: 全组统一)

1. 回测页全量跑一轮(只跑主品种, jobs 批);
2. 排名页筛: `oos_pass ✓ + 净点>0 + 最少笔数 30`(A 组放宽到 10);
3. 按总表末列分组统计**存活率** = 该组 oos_pass 数 / 该组生成数;
4. 判读:
   - 组间存活率对比 → 哪个流派在哪个品种/周期还有肉;
   - E 组是标尺(见总表);
   - 存活者进下一关: 参数邻域稳健性 + 对账可信度(match≥90 的模板族) → 定上 demo 名单。

## 纪律牌(练功期挂墙上)

- 小样本 = 未证实, 不是已证明 — 高周期组尤其注意;
- 对比三铁律: 统一时间 / 统一配置(trail 全 null) / 统一手数;
- 留出段是唯一裁判, 全样本好看没有用;
- 淘汰的归档记死因 — 负样本喂回 AI 提示词。
