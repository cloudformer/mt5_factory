# v0.7 point 漂移哨兵 【已完成 2026-07-25】

## 问题

trades.net_points 是**入库当时按 symbols.point 缓存的换算结果**;point 变了(改精度/换券商)
→ 老成交旧尺、新成交新尺,对账/归因全失真,且无任何报错。

## 方案(零新增数据, 零维护, 开销≈零)

每行成交自带审计材料(原始价格 entry/exit_price = 永存的账本, net_points = 缓存值):

1. **检测**:对账时(compute_reconcile)抽 首/中/末 三笔——它们本来就已加载在内存里,
   用 `方向 × (出场价-入场价) ÷ 当前point` 现算现比,偏差 >1 点 → 响应带 point_drift;
   不写任何东西、不建表、不存对比历史;不漂移=永远沉默;
2. **警告 UI**:人工策略分析·对账 tab 红条(与"数据落后"同位置),列抽样"存 X ≠ 算 Y";
3. **一键治愈**:`POST /trades/heal_points {symbol}` —— 同一公式 + 当前 point,
   一条 UPDATE 批量刷新该品种全部 net_points(**刷新缓存值,方法不变,原始价格永不动**,
   幂等重复执行结果相同)。新入库成交本来就用当前 point,新老自然归一。

## 验证

公式与真实数据逐笔核过(168/3533 案例: net_points = 纯价差换算, 精确匹配);
治愈 SQL 幂等;正常库(未漂移)红条不出现,页面零变化。
