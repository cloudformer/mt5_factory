# v1.1 策略生成 · 未完成部分(活跃)

> 由原 `v1.5_策略生成.md` 拆出(已完成部分见 `v0.5`);并接收 v0.3 / v0.4 的残留项。
> 铁律不破:模板逻辑=代码进git;UI/AI 控制的是参数子空间。

## A. 指标搭配控制(生成页参数覆盖)【可开工】

生成页参数空间表改为可编辑,每参数三种玩法:锁定 / 收窄范围 / 默认空间;
生成请求带 overrides → `random_combo` 在用户子空间采样。api generate 加 overrides,模板零改动。

## B. 规模化收敛漏斗(剩余两件)

- **B1 漏斗数字记录**:每批记录 生成N → 粗筛剩X → 双筛剩Y;Y/N=该搭配方向"含真率",批间对比定收敛方向。
  (交叉门槛已实现"幸存者才跨品种",本件只差把漏斗数字落库/展示)
- **B2 一键管线**:生成完自动回测 → 完成自动对够格者跨品种 → 出双筛榜单。
  基础已备:jobs 表(020)断点续跑 + 交叉门槛自动分层,管线=按顺序投递 jobs。

## C. AI 接入(依赖 A/B 数据; 衔接 v2.1/v2.2)

- 协议桩已留:`ai_generator_url` `/propose`(参数组合);
- 输入 = 漏斗含真率(B1) + 模板级分析(下方 D) + 单策略成绩单(v2.1 已上线);
- 产出参数走同一漏斗;产出模板代码走门禁(语法→冒烟→回测→人工git审查);
- DSL 量产轨设计已存档于 `v2.1_AI闭环.md`(AI 生成启动时一并实施)。

## D. 接收自 v0.4:维度一·模板级聚合分析(C 的输入)

按模板聚合全体实例回答"往哪个方向生成":双筛通过率 / 漏斗含真率 / 参数敏感度(幸存者聚在哪个参数区)/
品种适配画像。数据全在库(backtests oos+跨品种行 × strategies.params),纯读。
(成绩单 failed_neighbors 已部分覆盖"死参数区域",本件是它的模板级汇总版)

## E. 接收自 v0.3:回测质量小残留

- #2 分段最少笔数:OOS 训练段/留出段各自要够笔数(现只有全局 min_trades)
- #3 短样本警告:M1 跨度不足门槛的回测标"样本不足"
- §7 数量核实:回测预览数 vs 排名总数 口径差异,跑 4 条 SQL 正式关闭(部分原因已修:ARCHIVED 口径)

## 落地顺序建议

A → B1(跑第一批 2000 记录漏斗数字) → D(展示含真率) → B2 → C(与 v2.2 DSL 同期)。E 见缝插针。
