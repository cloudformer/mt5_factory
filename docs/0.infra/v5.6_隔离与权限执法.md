# v5.6 隔离与权限执法(第六步 · 唯一有变更风险的一步)

> 【已铺·识别层最小版 2026-07-25】api 中间件解析 X-API-Key(用户钥匙/worker 钥匙)→
> request.state.user_id/auth_kind + last_used_at + debug 日志 —— **只识别不限制,
> 对任何请求零拦截**(Frank 定: 目前最多"看到请求是谁")。runner 请求已带 WORKER_KEY。
>
> 【已通电·读路径过滤 2026-07-26】登录前先把"数据隔离"的读一半接上(无登录, 右上角随意切换):
> web 每次调 api 捎 `X-User-Id`(右上角身份, app.before_request 落 session)→ 中间件无钥匙时
> 采信(auth_kind=dev, 信任内网 web)→ 列表类读端点按 `services/identity.scope_uid` 过滤:
> **无身份=不过滤(runner/脚本照旧), owner(id 1)=不过滤(全局视野: 总视图/总报表/调试),
> 其他用户=只见自己资产**。已过滤: /hosts、/strategies/status、/strategies/orphans、
> /backtest/top(市场页传 market=1 = 明确跨用户视图, 不过滤)、/reconcile/summary、
> /trades/local(本人策略的单 或 本人 worker 账户的单)。管理员菜单仅 owner 可见(导航层)。
> frank 持全部资产 → 切 frank 与通电前逐字节一致; 新建 user = 空视图, 过渡平缓。
>
> 【补口 2026-07-26·lego 实测两处漏】① 回测 plan/run 集合口同样按 scope_uid 过滤
> (点名 strategy_ids 也只认自己的 — 报别人的 id 不数不跑); ② 按 id 的读入口守门:
> identity.assert_strategy_visible / assert_host_visible(不是你的 → 404, 不暴露存在性),
> 已接: /strategies/{id}/report(连带 ai_prompt/trail_prompt)、family、trail_compare、
> trail_batch、/backtest/results/{id}、/reconcile/{id}、/analysis/{id}、
> /hosts/{id}/events、/hosts/{id}/trades。管理台 /admin/* 只认 owner(web 门禁)。
> 登录/密码/401/写路径执法(set_status/set_trail 等)/shared 可见性裁剪仍未做, 等发真实 key 前接通。

> 序列见 v5.1。前 5 步把字段/表/页面全部备好且零行为变化;本步一次接通:
> **认证 → 数据隔离 → 可见性放行 → 配额闸门 → 管理权限**。
> **铁则:本步验收全绿之前,绝不发任何用户 key;做完这步,系统才是多租户。**

## 进度盘点(2026-07-26 与 Frank 定格 — 此处暂停, 转修内功; 缺的三块等发真实 key 前一批接)

| # | 项 | 进度 |
|---|---|------|
| 1 | 认证 | ~40%: 识别层全通(X-API-Key 解析 + X-User-Id 过渡身份); **缺**登录页/密码/401、worker 强制只认钥匙 |
| 2 | 数据隔离 | 读路径 ~90%: 列表类全过滤 + 回测 plan/run(含点名) + 按 id 九处守门 404; **写路径 0%**(set_status/set_trail/挂载等未执法) |
| 3 | 可见性放行 | ~50%: 市场页红线实测通过(lego 验收: shared 藏名藏参, public 全裸); **缺** shared 响应裁剪、private 按可见性 404、fork/订阅 |
| 4 | 配额 | 100%(按"只记录不拦截"口径, usage_counters 在跑) |
| 5 | 管理权限 | ~90%: owner 全局视野、/admin 门禁、菜单隐藏; **缺** admin 的 api 端点拦截(跟登录一批) |

验收项(越权扫描脚本/无 key 全 401)未做, 挂在同一闸门: **发第一把真实 key 之前**。
lego 实测遗留毛边(其对回测/分析页的体验)按 Frank 定: 暂停不追, 未来完善。

## 内容(按接通顺序)

1. **认证**:api 全局中间件 —— `X-API-Key` → api_keys 查哈希 → 请求上下文带 user_id;
   web 登录(Flask 内建 session 手写 ~30 行 + bcrypt,不上 Flask-Login,2026-07-25 定);
   无凭证=401,白名单只放 /health + /hosts/announce(它自带钥匙验证);
   **worker 用独立钥匙 = worker_keys 表**(schema/040 已铺,取代原 mt5_hosts.worker_key_hash
   列方案):每机一把、签发即扩容、announce 首绑一机一钥、吊销即解绑 —— 真金通道与
   用户 key 分离。**5.6 起强制**:拉任务/上报只认 worker 钥匙,自报主机名降为显示用;
   BRIDGE_API_KEY 保留(反方向 api→bridge 的门禁,库里只有哈希拿不出明文,不能复用);
   通信单向化(用户自带 worker/NAT 场景)见 v7.2;
2. **数据隔离**:所有查询按 `owner_id = 当前用户` 过滤 —— 收敛在一处依赖/中间件;
   runner 拉任务按 host.owner 过滤策略与配置;策略转 DEMO/LIVE 只认自己名下 worker;
3. **可见性放行**(按 v5.4 定死的语义):public 全可见+可 fork;shared 汇总可见+盲测;
   private 除 owner 外一律 404;**盲测=标准回测,引擎零分叉**——执法全在输出层:
   api 按可见性裁剪响应(shared 非 owner 只放行汇总视图,响应体断言无 trades),
   视图切换(回测/实盘 × 汇总/明细/交叉)变成权限问题而不是引擎问题;
4. **配额只记录不拦截(2026-07-25 Frank 定)**:usage_counters 表 + 四个入口
   (回测入队/AI报告/建策略/注册worker)原子计数(`ON CONFLICT DO UPDATE used=used+N`),
   页面可见用量 — **不做 429 拒绝**,足够成熟才考虑开限制(届时再议);
   user_config 可写白名单:quota_* 只允许 owner 写;
5. **管理权限**:角色只有两个(2026-07-25 定)—— **owner**(user 1:全量视角,
   看所有用户的 worker/策略/报表,管理页只认它)/ **user**(只见只管自己名下的一切)。
   过滤中间件里就一个分支: owner 放行全量, 其余按 owner_id 过滤;不做角色体系。
   **前瞻·租户内分权(2026-07-25 记, 未来有团队客户再做)**:user 层级拆
   role=admin(租户操作员: 管自己的 windows/挂载/策略/手数)/ role=viewer(挂靠账号:
   parent_user_id 指向租户 admin, 数据范围=挂靠对象, 权限=只读报表/对账/成绩)。
   实现仍不建组织表不做矩阵: 中间件多一条"viewer 用 parent 的 owner_id 查 + 写操作 403",
   模板一个 can_write 开关统一隐藏操作按钮 — 页面零分叉。

```sql
-- 本步唯一新表
CREATE TABLE usage_counters (
    user_id INTEGER NOT NULL REFERENCES users(id),
    metric  VARCHAR(32) NOT NULL,
    period  DATE NOT NULL,                -- 按天一行, 跨天自然归零(新行)
    used    INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (user_id, metric, period)
);
```

## 可选加固(建议第二阶段再开): 数据库 RLS

应用层过滤跑通、越权扫描全绿后,再开 Postgres 行级安全兜底
(`CREATE POLICY ... USING (owner_id = current_setting('app.user_id')::int OR visibility IN ('public','shared'))`)
—— 代码哪天漏写 WHERE,数据库直接不给行;与"唯一约束防重复"同一哲学。
代价是调试与少量性能,所以分两阶段。

## 风险与缓解(本步 = 全路线的命门)

- **隔离漏一处=数据泄漏;worker 归属错一行=下错账户** —— 这是唯一真正危险的一步;
- 缓解三件套(全部纳入常驻回归清单,与 v2.4 测试哲学同源):
  ① 过滤收敛一处;② **越权扫描脚本**(两个测试用户互扫全部 API,断言零越权);
  ③ 无 key 扫全路由脚本(非 401 即 bug);
- 发布顺序:先 user 1 自己带 key 跑一周(行为应与今天逐字节一致)→ 建测试用户跑扫描
  → 全绿 → 才发第一把真实用户 key。

## 风险 / 难度 / 验收

- 风险:**高**(集中承担全部行为变更);难度:高;
- 验收:无 key 全 401;越权扫描全绿;user 1 全站回归无差异;
  配额打满被拒且提示明确;shared 盲测响应无逐笔;A 的 worker 拉不到 B 的任何东西。
