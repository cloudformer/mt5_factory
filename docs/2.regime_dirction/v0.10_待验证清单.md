# v0.10 待验证清单 — v0.7 批次 1~3（代码已完成，未在服务器验证）

2026-08-08 记录。代码全绿（76 单测），但 Frank 在外网无法 `make up`，三批**未做部署验证**。
回内网后按下面用例逐条打勾，全过后本文件改名加「已完成」。

前置：`make up`（schema/064 建 tags 列、065 建 predictions 表 + config.prediction 种子）。

## 批次 1 — tags 筛选履历

- [ ] **T1 列在**：`\d strategies` 见 `tags | jsonb | not null | '[]'`
- [ ] **T2 放料回池**（池子已被 #4/#5 清空）：
  ```sql
  UPDATE strategies SET basis = NULLIF(regexp_replace(
      COALESCE(basis,''), '｜?oos_v2#[0-9]+', '', 'g'), '')
   WHERE id IN (774, 775, 776, 777, 778);
  ```
- [ ] **T3 跑一批**：/oos-v2 上限 5 开始（7 天内跑过 → 走复用秒完）→
  ```sql
  SELECT id, tags FROM strategies WHERE tags::text LIKE '%oos_v2#%';
  ```
  预期：5 行，元素三字段 `{"report": "oos_v2#N", "status": "pass|fail", "created_time": "…"}`
- [ ] **T4 出池**：/oos-v2 预估行不再包含这 5 只；basis 里**没有**新增 oos_v2 字样
- [ ] **T5 页面**：策略列表标签列见绿/红 `oos_v2#N·pass|fail` 徽字（悬停见时间）；
  搜「标签/生因」输 `pass` 能捞出

## 批次 2 — Profile 现拼

- [ ] **P1**：策略分析页载入 764 → 点「Profile JSON」→ 返回完整 dict
- [ ] **P2 同刀法对账**：`stability.windows` 里 20 年窗的 n/net/pf/dd 与 oos_v2 最新报告里
  764 的 `total` 逐字段一致（同一天看，同锚点同函数必须相等）
- [ ] **P3**：`states.cells` 各格 n 之和 ≤ 总笔数（无标签日的笔不计格，属正常）
- [ ] **P4**：`history.tags` = T3 里库里那份原样

## 批次 3 — 预测验证

- [ ] **R1 造对象**：找/造一个带门策略（metadata.regime 有 cells 的克隆实例，有回测行）
- [ ] **R2 冻结**：拉它的 Profile → `prediction` 非空，`expected` 有值；
  `SELECT * FROM predictions;` 一行，frozen_at = 该策略 created_at
- [ ] **R3 冻结执法**：再拉一次 Profile / 重跑该策略回测后再拉 → `expected` 逐字节不变
- [ ] **R4 成熟度**：新门 `days_validated` 小 → `verdict = "immature"`，
  reason 形如「未证实: 已验证 X 天 / Y 笔(门槛 90 天 · 20 笔)」
- [ ] **R5 判定口径抽查**（纯函数已有单测锁死，服务器上不用重验数学）：
  期望 1.5 → 实际 1.2 = 保持率 0.80 → 有效；实际 ≤1 → 失效

## 顺手项

- [ ] plan 里 `tagged` 恒为 0 的显示怪癖（条件自斥，v1 同款）— 无害，看着烦就修
