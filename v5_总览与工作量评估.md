# v5 总览与工作量评估(参考用主表)

> 只做索引 + 风险/工作量评估,不含设计内容(设计各在对应分册)。
> 2026-07-22 评估,供排期参考。**风险**=会不会搞坏现有系统;**难度**=纯工作量。

## 全序列

```
v5.0 worker拓扑与策略挂载   ← 地基, 可独立于多用户先上
v5.1 策略归属字段
v5.2 用户体系与worker归属
v5.3 license项进配置
v5.4 市场字段与区分
v5.5 用户管理后台
v5.6 隔离与权限执法          ← 命门, 所有行为变更集中于此
v5.7 订阅执行                ← 变现闭环, 依赖 5.6
```

总纲(全 v5 受约束):不分叉逻辑,引擎单一代码路径对所有用户一致,
差异只在 **UI 视图 / 权限 / 配额** 三个加法薄层(详见 v5.1)。

## 风险 / 工作量主表

| 文档 | 风险 | 难度 | 只加不变? | 关键改动点(风险来源) |
|---|---|---|---|---|
| v5.0 挂载 A段 | 零 | 低 | 是 | 建 strategy_mounts 表 + 回填【已完成 2026-07-24】 |
| v5.0 挂载 B1 | **中** | 中 | 否 | runner 认领改 host 指派 · stats 改主键按账户 · 手数搬挂载点【已完成并线上验证 2026-07-25】 |
| v5.0 挂载 B2 | 中 | 中 | 否 | 挂载管理 UI · 对账按账户切分(券商记分卡)· 盲测不落库 |
| v5.1 归属字段 | 极低 | 低 | 是 | 加 owner_id/visibility 列 + 默认值 |
| v5.2 用户体系 | 极低 | 低 | 是 | 加 users/api_keys/user_config 表 + mt5_hosts.owner_id,无人读 |
| v5.3 license进配置 | 极低 | 低 | 是 | 塞 quota_* 配置键,无人读 |
| v5.4 市场字段 | 低 | 低-中 | 是 | 加 origin_id + 列表加列 + 只读市场页雏形 |
| v5.5 管理后台 | 低 | **中** | 是 | 对现有零改动,但整个后台+若干端点是实打实工作量 |
| v5.6 隔离执法 | **高** | **高** | 否 | 认证+全量查询过滤+可见性放行+配额闸门(+RLS);漏一处=数据泄漏 |
| v5.7 订阅执行 | 中-高 | 中 | 否 | 真金代跑;复用 v5.0 挂载+5.6 参数保护,新增订阅关系管理+托管运营 |

## 三档归类(排期用)

- **真·零风险,可放心先铺**:v5.0-A、v5.1、v5.2、v5.3(纯加法,没人读新东西);
- **低风险但有工作量**:v5.4、v5.5(不碰现有逻辑,但是新页面/端点,费工不费险);
- **要小心验收的**:v5.0-B(碰实盘+统计+对账)、v5.6(命门)、v5.7(真金代跑)。

## 执行顺序(2026-07-24 定版, 风险分层≠执行顺序, 还要叠"何时真的需要")

- **阶段 1|一次部署铺地基**:5.0-A + 5.1 + 5.2 + 5.3 —— 【已铺 2026-07-24,
  schema/033~036,幂等/回填/守卫经一次性 postgres16 容器全链验证】
  验收一条:全站回归无差异(新列新表无人读)。新策略自动 owner=1/private,零感知;
- **阶段 2|按真实驱动力二选一**:
  - 想先扩自己算力(多台 demo 机分担、多账户)→ **5.0-B**(全序列唯一对单用户
    现在就有价值的一步)——**B1 已完成并线上验证 2026-07-25**(挂载认领管道通,
    随时可加机器,新策略自动流向最少负载的一台);B2(挂载 UI+对账按账户)待做;
  - 想先给人看/准备开放 → **5.4 市场页 + 5.5 管理后台**(费工不费险);
- **阶段 3|5.6 卡在"发第一把 key 之前"做,不早不晚**:早做=没人用还碍事;
  晚做=事故。触发条件只有一个:决定给第一个真实用户发 key。
  验收=无 key 全 401 + 越权扫描全绿 + user1 全站回归无差异;
- **阶段 4|5.7 订阅执行最后**:硬前置 5.6(参数保护)+ 5.0-B(订阅=挂载行)。

铁则:每阶段独立可部署可停;阶段 2 的分叉到时候哪个需求先来走哪条腿。
