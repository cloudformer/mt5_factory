测试 1:正常机器不受影响(worker01 或 worker03)

先在正常的机器上验,确认没搞坏。

git pull  →  重启 bridge

看两处:

┌──────────────────────────────┬───────────────────────────────────────────────┐
│            看哪里            │                    应该是                     │
├──────────────────────────────┼───────────────────────────────────────────────┤
│ 状态页 http://<win-ip>:8020/ │ MT5 已连接、账户照旧显示                      │
├──────────────────────────────┼───────────────────────────────────────────────┤
│ bridge 窗口日志              │ 出现 MT5 connected(附着已登录会话): login=... │
└──────────────────────────────┴───────────────────────────────────────────────┘

关键就是日志里那个「附着已登录会话」 —— 说明走的是新的第 2 步,没去动终端里的会话。

---
测试 2:失败原因能看见(worker02,现在未登录的那台)

git pull  →  重启 bridge

打开 Linux 的 Workers 页 → 展开 worker02 → 看 「MT5 账户」 那一行:

MT5 账户   [未登录]   未登录, 请检查: 终端里没有账号, env 也没配 MT5_LOGIN
                     三件套 — 上机在 MT5 界面登录一次, 或在 env 填三件套让它自动登录

改之前那一格是空的,只有「未登录」三个字。

---
测试 3:手工登录后自动恢复

在 worker02 的 MT5 终端界面手工登录账号。

什么都别重启,等 30 秒(bridge 的重连循环):

┌────────────┬───────────────────────────────────────────┐
│   看哪里   │                  应该是                   │
├────────────┼───────────────────────────────────────────┤
│ Workers 页 │ MT5 已连接,右格从错误原因变回 USD         │
├────────────┼───────────────────────────────────────────┤
│ runner     │ 从 still waiting 出来,策略变成 66/66 一致 │
└────────────┴───────────────────────────────────────────┘

---
测试 4(可选):env 凭据自愈

只有想让终端账号丢了也能自己恢复时才做。

在 worker02 的 env/.dev.env 里加(注意:不要写行内注释):

MT5_LOGIN=109562647
MT5_PASSWORD=那台的密码
MT5_SERVER=MetaQuotes-Demo

重启 bridge → 日志应出现:

终端未登录, 用 env 凭据登录 login=109562647 server=MetaQuotes-Demo
MT5 connected(env 凭据登录): ...

填错了也不怕 —— 会显示 未登录, 请检查: env 凭据登录失败(login=… server=…),而且不会踹掉终端里已有的好会话(这正是本次改动的核心)。

---
出问题怎么退

git checkout windows_mt5/bridge/main.py containers/web/templates/_macros.html env/.dev.env.example
然后重启 bridge。

---
一句话判断标准

- 测试 1 的日志里有「附着已登录会话」 → 新顺序生效,正常机器安全
- 测试 2 的 Workers 页看到原因文字 → 原因外露链路通(bridge → 心跳 → Linux 页面)

这两条过了,改动就算成功。