# v2.3 多券商户口制 · 验收清单(2026-08-19)

## list_v1 存量回归(单券商世界)

**核心判据只有一条:所有数字和部署前一模一样。**

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| 1 | 排名页开一遍(默认排序+健壮性列) | 名次、成绩、健壮性比例与部署前一致 | |
| 2 | 点开 11806 的健壮性明细/各品种行 | 每品种一行照旧,没有多余行 | |
| 3 | 单策略分析页(11806):汇总、资金曲线、regime 色带 | 笔数/净点/八格归因不变 | |
| 4 | Regime 页 + 九币矩阵 + 当日格 | 时间线正常,当日格照出 | |
| 5 | 按 ID 点名回测 11806(同窗口重跑) | 成绩与库里现有行一致(悲观撮合未被碰) | |
| 6 | 勾「跨品种验证」跑一个小批 | 预览/明细行为照旧;**不勾时**预览数字与之前完全一样 | |
| 7 | OOS v2 报告跑一次(门票三个) | 结论不变(11806/11810/11809 照旧) | |
| 8 | 对账页开 FABLE | 复现率与上次一致 | |
| 9 | 预测板、策略生成页列表 | 正常出数、成绩列正常 | |
| 10 | 下载页覆盖统计 + 手动触发一次增量同步 | 每品种 M1 行数只增不变形;同步正常跑完 | |
| 11 | 配置页 | 默认券商显示 `MetaQuotes-Demo`,其余配置项齐全 | |
| 12 | Workers 页 + runner 机策略状态 | 心跳正常;fable/weekly_day 状态绿(前提:worker 已 git pull 重启) | |

## list_v2 第二券商功能(按顺序做,前一步绿了再下一步)

| # | 操作 | 预期 | 结果 |
|---|------|------|------|
| 0 | 前置:MT5 终端开 IC demo 三件套;起一台挂 IC 账户的下载 worker | workers 页出现新机,`mt5_server` 自动填 IC 服务器名 | |
| 1 | 登记:品种名按 IC 终端里的写,券商框手输 IC 服务器名(一字不差) | 1~2 分钟校验变绿;这任务**不会**派给 MetaQuotes worker | |
| 2 | 投毒反测:登记一个 IC 没有的假品种名@IC | 只有 IC 行标 verify_error;MetaQuotes 世界零影响 | |
| 3 | 下载:先拉一两年短窗 | 覆盖统计出现 `@IC` 独立行;MetaQuotes 行数一根不变 | |
| 4 | 幂等:同段再拉一次 | 行数不变(主键去重空转) | |
| 5 | 跨券商验证:点名 FABLE 三个 + 勾「跨券商验证」 | 预览显示「+ 跨券商 3」;跑完健壮性明细多出 IC 行;**主行成绩和排名一个数都不动**(关键验收位) | |
| 6 | 读数:比同策略两行的净点/PF/笔数 | 差异小 → 数据源差异可忽略,不必大规模下载别家;差异大 → 单独讨论 | |
| 7 | 迁户口演练(拿一个 CANDIDATE 尸体):migrate_broker 到 IC → 重跑回测 → 迁回 | 迁后排名显示未回测,重跑出新主行;旧行自动变验证行 | |
| 8 | 挂载警示:把策略挂到 server 名与户口不符的机器 | 返回 broker_warning 提示(不拦截) | |

> v2 的 5、6 是这次工程的全部意义:同一把尺子,换一家的数据,看结论稳不稳。
> 做到第 6 步就有了"要不要多券商全量数据"的实证答案。
